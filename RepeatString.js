function repeat(str, num) {
    //let repeatString = (str, num) => str.repeat(num);
    let repeatString = (str, num) => { return str.repeat(num) };
    console.log(repeatString(str, num));
}
repeat("abc", 3)